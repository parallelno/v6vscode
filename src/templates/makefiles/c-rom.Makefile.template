ROM  = out/{{name}}.rom
ASM  = out/{{name}}.asm
SRC  = src/main.c
CPU  = i8080

V6ASM ?= v6asm

$(ROM): $(ASM)
	$(V6ASM) $(ASM) -o $(ROM) -c $(CPU)
$(ASM): $(SRC)
	v6c $(SRC) -o $(ASM)
clean:
	rm -f $(ASM) $(ROM)
